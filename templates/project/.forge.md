---
name: {project-name}
type: {desktop-utility | discord-bot | web-app | website | automation | game | ai-tool | other}
status: idea
scope: weekend
stack: tbd
created: {YYYY-MM-DD}
forge_version: 0.2.0
forge_repo: {absolute path to your project-forge-os repo — auto-filled from FORGE_OS_HOME if set, e.g. C:\dev\project-forge-os}
---

# Forge metadata

Machine-readable project state. Skills read and update the frontmatter above;
you can edit it by hand at any time.

**Field reference**

- `status`: `idea` → `active` → (`paused` | `maintenance`) → `shipped` | `parked` | `killed`
- `scope`: `weekend` (default — a few sessions), `mvp` (a few weeks), `product` (long-lived)
- `stack`: short slug once chosen, e.g. `tauri-react-ts`, `python-script`
- `forge_repo`: where skills find the global `memory/` folder. Auto-filled
  from the `FORGE_OS_HOME` environment variable when `init-project` creates
  this file, if it's set. If the path is wrong or missing, skills work
  without memory and say so — `doctor` checks this too.
